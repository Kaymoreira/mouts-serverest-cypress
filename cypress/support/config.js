// Central place for environment endpoints. Importable from any spec/support
// module, so switching environments is a one-line change here.
// (Cypress 16 removed Cypress.env(), so a plain module is the portable option.)
export const API_URL = 'https://serverest.dev';
